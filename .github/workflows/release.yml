name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release'
        required: true

jobs:

  initial_release:
    name: Create base release ${{ github.event.inputs.version }}
    runs-on: ubuntu-latest
    outputs:
      crate-version: ${{steps.get-crate-version.outputs.crate-version}}
      commit: ${{steps.make-commit.outputs.commit}}
    steps:    
      -
        name: Checkout code
        uses: actions/checkout@v3
        with:
          submodules: true
      -
        name: Get crate version
        id: get-crate-version
        run: echo ::set-output name=crate-version::``$(echo ${{ github.event.inputs.version }} | cut -dv -f2)``
        shell: bash    
      -
        name: Bump crate version
        uses: thomaseizinger/set-crate-version@master
        with:
          version: ${{steps.get-crate-version.outputs.crate-version}}   
      -
        name: Create release branch
        run: git checkout -b release/${{steps.get-crate-version.outputs.crate-version}}
      - 
        name: Update changelog
        uses: thomaseizinger/keep-a-changelog-new-release@1.1.0
        with:
          version: ${{steps.get-crate-version.outputs.crate-version}}
      - 
        name: Initialize mandatory git config
        run: |
          git config user.name "GitHub actions"
          git config user.email noreply@github.com 
      - 
        name: Commit changelog and manifest files
        id: make-commit
        run: |
          git add CHANGELOG.md Cargo.toml
          git commit --message "Prepare release ${{steps.get-crate-version.outputs.crate-version}}"
          echo "::set-output name=commit::$(git rev-parse HEAD)"
        shell: bash
      - 
        name: Push new branch
        run: |
          echo "your token is ${GITHUB_TOKEN}."
          git push origin release/${{steps.get-crate-version.outputs.crate-version}} 
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
      -
        name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.event.inputs.version }}
          release_name: Release ${{ github.event.inputs.version }}
          draft: true
          prerelease: true

  release_create:
    name: Release crate ${{ github.event.inputs.version }}
    runs-on: ubuntu-latest
    needs: initial_release
    steps:
      -
        name: Checkout code
        uses: actions/checkout@v3
        with:
          submodules: true
      -
        name: Bump crate version
        uses: thomaseizinger/set-crate-version@master
        with:
          version: ${{needs.initial_release.outputs.crate-version}}
      -
        name: Install toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: x86_64-unknown-linux-gnu
          override: true
          default: true
      -
        name: Publish crate
        run: cargo publish --allow-dirty
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  release_container:
    name: Release container ${{ github.event.inputs.version }}
    runs-on: ubuntu-latest
    needs: initial_release
    steps:
      -
        name: Checkout code
        uses: actions/checkout@v3
        with:
          submodules: true
      -
        name: Bump crate version
        uses: thomaseizinger/set-crate-version@master
        with:
          version: ${{needs.initial_release.outputs.crate-version}}
      -
        name: Login to ghcr.io
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      -
        name: Build and push container
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/timvw-hello-rs:${{ github.event.inputs.version }}


  release_assets:
    name: Release assets ${{ github.event.inputs.version }} (${{ matrix.config.toolchain }}-${{ matrix.config.target }}-${{ matrix.config.target_cpu }})
    runs-on: ${{ matrix.config.os }}
    needs: initial_release
    strategy:
      fail-fast: false
      matrix:
        config:
          # Linux
          - {os: ubuntu-latest, toolchain: stable, target: x86_64-unknown-linux-gnu, target_cpu: generic, cross: false}
          - {os: ubuntu-latest, toolchain: stable, target: x86_64-unknown-linux-gnu, target_cpu: broadwell, cross: false}
          - {os: ubuntu-latest, toolchain: stable, target: aarch64-unknown-linux-gnu, target_cpu: generic, cross: true}
          - {os: ubuntu-latest, toolchain: stable, target: arm-unknown-linux-gnueabi, target_cpu: generic, cross: true}
          - {os: ubuntu-latest, toolchain: stable, target: armv7-unknown-linux-gnueabihf, target_cpu: generic, cross: true}
          # mips and powerpc are not compitble with ring v0.16.10, so they are disabled for now
          #          - {os: ubuntu-latest, target: mips64el-unknown-linux-gnuabi64, cross: true}
          #          - {os: ubuntu-latest, target: powerpc64le-unknown-linux-gnu, cross: true}
          - {os: ubuntu-latest, toolchain: stable,  target: x86_64-unknown-linux-musl, target_cpu: generic, cross: true}
          - {os: ubuntu-latest, toolchain: stable,  target: x86_64-unknown-linux-musl, target_cpu: broadwell, cross: true}
          - {os: ubuntu-latest, toolchain: stable, target: aarch64-linux-android, target_cpu: generic, cross: true}
          # Macos
          - {os: macos-latest, toolchain: stable, target: x86_64-apple-darwin, target_cpu: generic, cross: false}
          - {os: macos-latest, toolchain: stable, target: x86_64-apple-darwin, target_cpu: broadwell, cross: false}
          # Windows
          - {os: windows-latest, toolchain: stable-x86_64-pc-windows-gnu, target: x86_64-pc-windows-gnu, target_cpu: generic, cross: false}
          - {os: windows-latest, toolchain: stable-x86_64-pc-windows-msvc, target: x86_64-pc-windows-msvc, target_cpu: generic, cross: false}
          - {os: windows-latest, toolchain: stable-x86_64-pc-windows-gnu, target: x86_64-pc-windows-gnu, target_cpu: broadwell, cross: false}
          - {os: windows-latest, toolchain: stable-x86_64-pc-windows-msvc, target: x86_64-pc-windows-msvc, target_cpu: broadwell, cross: false}
    steps:
      -
        name: Install toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.config.toolchain }}
          target: ${{ matrix.config.target }}
          override: true
          default: true
      -
        name: Downgrade cross
        uses: actions-rs/cargo@v1
        if: ${{ matrix.config.cross }}
        with:
          command: install
          args: --version 0.1.16 cross
      -
        name: Checkout code
        uses: actions/checkout@v3
        with:
          submodules: true
      -
        name: Install edit
        uses: actions-rs/install@v0.1
        with:
          crate: cargo-edit
          version: 0.9.1
          use-tool-cache: true
      -
        name: Update version
        run: cargo set-version ${{needs.initial_release.outputs.crate-version}}
      -
        name: Build application
        uses: actions-rs/cargo@v1
        with:
          use-cross: ${{ matrix.config.cross }}
          command: rustc
          args: --manifest-path ./Cargo.toml --release --target ${{ matrix.config.target }} --no-default-features -- -C target-cpu=${{ matrix.config.target_cpu }}
      -
        name: Pack binaries if unix
        if: matrix.config.os != 'windows-latest'
        run: tar -C ./target/${{ matrix.config.target }}/release -czvf timvw-hello-rs-${{ github.event.inputs.version }}-${{ matrix.config.target }}-${{ matrix.config.target_cpu }}.tar.gz timvw-hello-rs
      -
        name: Pack binaries if windows
        if: matrix.config.os == 'windows-latest'
        run: compress-archive ./target/${{ matrix.config.target }}/release/timvw-hello-rs.exe timvw-hello-rs-${{ github.event.inputs.version }}-${{ matrix.config.target }}-${{ matrix.config.target_cpu }}.zip
      -
        name: Upload binaries to release
        uses: svenstaro/upload-release-action@v1-release
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: timvw-hello-rs-${{ github.event.inputs.version }}-${{ matrix.config.target }}-${{ matrix.config.target_cpu }}.*
          asset_name: timvw-hello-rs-${{ github.event.inputs.version }}-${{ matrix.config.target }}-${{ matrix.config.target_cpu }}
          tag: ${{ github.event.inputs.version }}
          file_glob: true
          overwrite: true

# not needed - renovate bot on timvw/homebrew-tap repo will do this for me.
#  update_homebrew_formula:
#    name: Bump Homebrew formula
#    runs-on: ubuntu-latest
#    needs:
#      - initial_release
#      - release_assets
#    steps:
#      - name: Update formulate
#        uses: mislav/bump-homebrew-formula-action@v2
#        with:
#          homebrew-tap: timvw/homebrew-tap
#          base-branch: master
#          formula-name: timvw-hellors-bin
#          tag-name: ${{needs.initial_release.outputs.crate-version}}
#          download-url: https://github.com/timvw/timvw-hello-rs/releases/download/${{needs.initial_release.outputs.crate-version}}/timvw-hello-rs-${{needs.initial_release.outputs.crate-version}}-x86_64-apple-darwin-generic.tar.gz
#        env:
#          COMMITTER_TOKEN: ${{ secrets.COMMITTER_TOKEN }}